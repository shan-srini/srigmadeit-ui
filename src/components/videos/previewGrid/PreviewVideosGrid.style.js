import { makeStyles } from '@material-ui/core'

const style = makeStyles((theme) => ({
    previewVideosGridContainer: {
        backgroundColor: '#3F3F3F',
        margin: theme.spacing(1),
        padding: theme.spacing(1),
        borderRadius: '15px'
    }
}))

export default style;